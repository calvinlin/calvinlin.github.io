function saveInfo() {
	//parse form into JSON object
	//save into 


	var elemArr = [];
	var pos 	= 0;
	document.cookie = "C_split=" + saveStr;

	$('.person').each(function() {
		tempStr += 'index:' 
		
		elemArr[pos++] = tempStr;
	});
}



